{% render 'site-header', section: section %}

{% schema %}
{
  "name": "Header",
  "tag": "div",
  "class": "shopify-section-header",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "Primary menu"
    },
    {
      "type": "link_list",
      "id": "secondary_menu",
      "label": "Utility menu"
    },
    {
      "type": "text",
      "id": "support_label",
      "label": "Support link label",
      "default": "Support"
    },
    {
      "type": "text",
      "id": "support_phone_label",
      "label": "Support phone label",
      "default": "Call us"
    },
    {
      "type": "text",
      "id": "support_phone",
      "label": "Support phone number"
    },
    {
      "type": "url",
      "id": "support_url",
      "label": "Support link URL"
    },
    {
      "type": "checkbox",
      "id": "show_mega_panel",
      "label": "Show mega panel on desktop",
      "default": true
    },
    {
      "type": "header",
      "content": "Mega menu featured product"
    },
    {
      "type": "product",
      "id": "mega_menu_featured_product",
      "label": "Featured product",
      "info": "Shown in the bottom strip of the Shop dropdown. Leave blank to hide the strip."
    },
    {
      "type": "text",
      "id": "mega_menu_featured_label",
      "label": "Label",
      "default": "NEW",
      "info": "Pill label above the product name. Keep under 8 characters."
    }
  ],
  "blocks": [
    {
      "type": "featured_link",
      "name": "Featured link",
      "limit": 4,
      "settings": [
        {
          "type": "text",
          "id": "eyebrow",
          "label": "Eyebrow",
          "default": "Featured"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Navigation destination"
        },
        {
          "type": "textarea",
          "id": "copy",
          "label": "Copy",
          "default": "Short summary for a highlighted destination."
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ]
}
{% endschema %}
