<div class="checkout-trust-strip" role="list" aria-label="Checkout assurances">
  <span class="checkout-trust-strip__item" role="listitem">
    <span class="checkout-trust-strip__icon" aria-hidden="true">{% render 'icon', name: 'shield' %}</span>
    Secure checkout
  </span>
  <span class="checkout-trust-strip__item" role="listitem">
    <span class="checkout-trust-strip__icon" aria-hidden="true">{% render 'icon', name: 'truck' %}</span>
    Free returns
  </span>
  <span class="checkout-trust-strip__item" role="listitem">
    <span class="checkout-trust-strip__icon" aria-hidden="true">{% render 'icon', name: 'shield' %}</span>
    2-year warranty
  </span>
</div>
